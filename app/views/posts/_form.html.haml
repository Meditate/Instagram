#form
  = simple_form_for(@post) do |f|
    = f.error_notification

    .form-inputs
      = f.input :description
      = f.input :photo
      = f.input :user_id

    .form-actions
      = f.button :submit
      .pull-right
        = link_to('Back', posts_path, class:'btn btn-info')